[pytest]
markers =
    sanity: Quick health checks — row counts, data types, schema basics
    functional: Business logic — nulls, regex, dates, referential integrity, aggregates, composite keys
    data_quality: Data cleanliness — duplicates, numeric ranges, string lengths, outliers, partitions
    regression: Pipeline integrity — schema drift, file integrity, MD5 checksum
